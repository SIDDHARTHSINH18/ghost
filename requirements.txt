# GHOST backend dependencies.
#
# Install:  pip install -r requirements.txt
# Dev:      pip install -r requirements-dev.txt
#
# The legacy Gradio app (legacy/gradio-app) has its own
# requirements file and is not needed to run GHOST.

# API server
fastapi>=0.115
uvicorn>=0.30
python-multipart>=0.0.9

# External model provider
httpx>=0.27

# Configuration
python-dotenv>=1.0

# Document processing
pypdf>=4.0
python-docx>=1.1

# Retrieval (embeddings + TF-IDF)
sentence-transformers>=3.0
scikit-learn>=1.4
